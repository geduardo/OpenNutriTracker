/// PAL (Physical Activity Level) selection for onboarding.
/// This is NOT exercise tracking — it's the user's general daily activity level.
enum UserActivitySelectionEntity {
  sedentary,
  lowActive,
  active,
  veryActive;
}
